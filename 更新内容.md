## V5.0

1. 禁用根目录悬停背景变化和右移动画。
1. 增加了根目录的中文字体——“站酷小薇LOGO体”。
1. 修复了MAC系统交通灯遮挡侧边栏的BUG。
1. 美化了侧边栏顶部两个按钮的样式，改为液态玻璃的滑块效。（Windows）
1. 修复了脚注在悬浮时无法正常显示的BUG。

## V5.5

1. 隐藏了文件列表视图的md文件后缀。
2. 修改了文件树的小三角颜色。
3. 美化了五个不同的callout（警告框）。
4. 修改了文件列表视图激活项的标题文字颜色。
5. 修改了暗色模式下链接悬浮时的颜色。
6. 修复了mermaid图表的BUG。
7. 调整了脚注的大小和位置。
8. 修复了MAC系统的大纲溢出和侧边栏边框。

## V6.0

1. 更换了更契合主题风格YAML头。
2. 优化脚注。
3. 修复callout内的代码块颜色嵌套。
4. 优化亮色主题下侧边栏顶部的滑块（汉白玉）。
5. 优化了侧边栏阴影。
6. 修复了侧边栏底栏组件背景溢出。

## V6.5

1. 优化了暗色主题下代码块类型选择窗口的阴影。
2. 将列表视图的底栏改为背景透明。
3. 去除侧边栏拖动触发区的hover背景。
4. 统一文件列表视图加载区的背景和加载条。
5. 优化右键菜单、增加入场微动效。
6. 优化搜索面板。

## V7.0

> [!CAUTION]
>
> 本次更新涉及一体化窗口和侧边栏收展表现。macOS 用户请谨慎更新，建议先备份现有主题文件。

1. 隐藏右键菜单的选项提示。
2. 隐藏右键菜单的伪边框按钮阴影。
3. 微调了任务列表。
4. 适配了一体化。
5. 提升了侧边栏收展的流畅度。

## V7.5

1. 优化了一体化主内容区的细节（megamenu 滑块层淡入、recent-document hover、菜单字体）。
2. 修复了表格尺寸调整工具栏（md-grid-board）的网格渲染问题，新增 accent 色高亮。

## V8.0

1. 去除「文件 / 大纲」按钮的 active 状态背景。
2. 去除底栏根目录的 hover 背景。
3. 设置界面翻新：导航项、按钮、输入框、搜索框统一圆角与 accent 色边框。
4. 新增一体化 / 全屏标题栏背景，避免滚动时正文从顶部透明区域透出。
5. macOS 端基线修正：
   - 亮色主题字重对齐至 360 / 530（与暗色统一）。
   - 亮色文件激活项颜色改为 `var(--LOGO-color)`（与 logo 一致）。
   - 暗色 `strong` 去掉单独着色（贴合 Claude 原生）。
   - 暗色代码块 `cm-def` 从不可见的 `#0051c2` 改为 `#79b8ff`。

## V9.0 ~ V11.0（合并上游 2025-05 最新）

上游变更：
1. 优化打印态的下划线和高亮样式显示。
2. 增加书写区顶部的渐隐遮罩。
3. 微调底栏。
4. 修复全屏模式下应用栏顶部"漏风"。
5. 优化侧边栏的搜索框。
6. 调整文字选中的颜色。
7. 矫正还原了嵌套列表。
8. 优化了文件树视图的搜索框动画。
9. 增加了侧边栏下拉搜索框时滑块的动效。
10. 更新了暗色主题下行内代码的字体颜色。
11. 取消了文件树节点和大纲节点的 hover 背景。
12. 微调任务列表的复选框。
13. 修复了暗色主题下公式块的 selection 背景。
14. 删除了 outline。
15. 实现了大纲节点长文本标题的自动换行。
16. 实现了文件树节点长文本标题的自动换行。
17. 修复了文件夹节点异常展开。

macOS 端基线保持：
- 亮色字重 360 / 530，暗色字重 360 / 530。
- 亮色文件激活项 `var(--LOGO-color)`。
- 暗色 `strong` 无单独着色。
- 暗色 `cm-def` 使用 `#79b8ff`。
